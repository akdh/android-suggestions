<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.survey"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="8"
        android:targetSdkVersion="10" />

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name="com.example.survey.DisplayContext"
            android:label="@string/title_activity_display_context" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.example.survey.Suggestion"
            android:label="@string/title_activity_suggestion" >
        </activity>
        <activity
            android:name="com.example.survey.SuggestionList"
            android:label="@string/app_name" >
        </activity>
        <activity
            android:name="com.example.survey.Finish"
            android:label="@string/title_activity_finish" >
        </activity>
    </application>

</manifest>
